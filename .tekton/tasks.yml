---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: test
spec:
  steps:
    - name: run-tests
      image: python:3.9
      script: |
        #!/bin/sh
        pip install nose
        echo "Running unit tests..."
        nosetests simple-interest.py
